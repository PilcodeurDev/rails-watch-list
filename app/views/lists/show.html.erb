<p style="color: green"><%= notice %></p>

<%= render @list %>

<div>
  <%= link_to "Back to lists", lists_path %>
  <%= link_to "add movies", new_list_bookmark_path(@list) %>


  <%= button_to "Destroy this list", @list, method: :delete %>
</div>
